#!/bin/bash -e
# set Odoo admin password

. /etc/default/inithooks

[ -e "$INITHOOKS_CONF" ] && . "$INITHOOKS_CONF"
[ -e "/etc/turnkey-podman/.env" ] && . "/etc/turnkey-podman/.env"

export POSTGRES_USER POSTGRES_DB
"$INITHOOKS_PATH/bin/odoo.py" --pass="$APP_PASS"
