ServerName localhost

<VirtualHost *:80>
        RewriteEngine on
        ReWriteCond %{SERVER_PORT} !^443$
        RewriteRule ^/(.*) https://%{HTTP_HOST}/$1 [NC,R=301,L]
</VirtualHost>

<VirtualHost *:443>
    SSLEngine on
    ServerAdmin webmaster@localhost

    ProxyRequests Off

    <Proxy *>
        Order deny,allow
        Allow from all
    </Proxy>
    
    ProxyVia On

    # Odoo's gevent worker serves websocket traffic separately from HTTP.
    ProxyPass /websocket ws://127.0.0.1:8072/websocket retry=0 timeout=200
    ProxyPassReverse /websocket ws://127.0.0.1:8072/websocket

    ProxyPass / http://127.0.0.1:8069/ timeout=200
    ProxyPassReverse / http://127.0.0.1:8069/

    RequestHeader set "X-Forwarded-Proto" "https"

    # Fix IE problem (http error 408/409)
    SetEnv proxy-nokeepalive 1
</VirtualHost>
